"""MCP server entrypoint — registers typed, read-only forensic tools.

No generic shell/execute tool is ever registered here: destructive operations
are impossible by construction. Filled in PHASE 5. See COMMIT CHECKPOINT 5.
"""


def main() -> None:
    """Console-script entrypoint (`sift-mcp`). Wired up in PHASE 5."""
    raise SystemExit("sift-mcp server not yet implemented (arrives in PHASE 5).")


if __name__ == "__main__":
    main()
